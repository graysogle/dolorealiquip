# Assuming infix_attribute is already defined as a list
infix_attribute = []

# Define the variable 'invisible'
invisible = "true"  # or "false", or any other value you want

# Append the formatted attribute string to the list
infix_attribute.append(f"invisible='{invisible}'")

# Print the list to see the result
print(infix_attribute)
